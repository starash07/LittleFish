<Window x:Class="LittleFish.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置"
        Width="720"
        Height="620"
        MinWidth="640"
        MinHeight="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <DropShadowEffect x:Key="WindowShadow" BlurRadius="30" ShadowDepth="0" Opacity="0.18" Color="#667085" />
        <DropShadowEffect x:Key="PanelShadow" BlurRadius="20" ShadowDepth="0" Opacity="0.10" Color="#667085" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#344054" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D0D5DD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#101828" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Chrome"
                                CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#84CAFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="78" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D0D5DD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#1D2939" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                CornerRadius="14"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#F9FAFB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#EAECF0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowCloseButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D0D5DD" />
            <Setter Property="Foreground" Value="#667085" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Width="34"
                                Height="34"
                                CornerRadius="17"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#FFF1F3" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#FECDD6" />
                                <Setter Property="Foreground" Value="#C01048" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#FFE4E8" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#FDA4AF" />
                                <Setter Property="Foreground" Value="#A11043" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#344054" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="SliderThumbStyle" TargetType="Thumb">
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbChrome"
                                CornerRadius="9"
                                Background="#FFFFFF"
                                BorderBrush="#74C8FF"
                                BorderThickness="2">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.18" Color="#74C8FF" />
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbChrome" Property="Background" Value="#F7FCFF" />
                                <Setter TargetName="ThumbChrome" Property="BorderBrush" Value="#4DB7FF" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbChrome" Property="Background" Value="#EAF7FF" />
                                <Setter TargetName="ThumbChrome" Property="BorderBrush" Value="#2EA8FF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderDecreaseButton" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Height="8"
                                CornerRadius="4"
                                Background="#9DDBFF" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderIncreaseButton" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Height="8"
                                CornerRadius="4"
                                Background="#FFFFFF"
                                BorderBrush="#D7EEFF"
                                BorderThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Height" Value="34" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="IsMoveToPointEnabled" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource SliderDecreaseButton}" Command="Slider.DecreaseLarge" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource SliderIncreaseButton}" Command="Slider.IncreaseLarge" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SliderThumbStyle}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ColorPreview" TargetType="Border">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="BorderBrush" Value="#D7EEFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>

        <Style x:Key="ValueTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Width" Value="58" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="#F7FCFF" />
            <Setter Property="BorderBrush" Value="#D7EEFF" />
            <Setter Property="Foreground" Value="#1570EF" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="10,0,0,12" />
        </Style>

        <Style x:Key="FontSizeTextBox" TargetType="TextBox" BasedOn="{StaticResource ValueTextBox}">
            <Setter Property="Width" Value="72" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="8,0" />
        </Style>

        <Style x:Key="StepButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="34" />
            <Setter Property="MinWidth" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="#F7FCFF" />
            <Setter Property="BorderBrush" Value="#D7EEFF" />
            <Setter Property="Foreground" Value="#1570EF" />
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="10" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True"
                                   Focusable="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Opacity="0"
                                                  Focusable="False" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Opacity="0"
                                                  Focusable="False" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb"
                                           MinHeight="34"
                                           Background="#CBD5E1">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="ThumbChrome"
                                                        Width="6"
                                                        HorizontalAlignment="Center"
                                                        CornerRadius="3"
                                                        Background="{TemplateBinding Background}" />
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbChrome" Property="Background" Value="#94A3B8" />
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbChrome" Property="Background" Value="#64748B" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0"
                                    HorizontalAlignment="Left"
                                    Padding="4"
                                    CornerRadius="18"
                                    Background="#EEF2F6"
                                    BorderBrush="#EAECF0"
                                    BorderThickness="1"
                                    Margin="0,0,0,18">
                                <TabPanel IsItemsHost="True"
                                          KeyboardNavigation.TabIndex="1"
                                          Background="Transparent" />
                            </Border>
                            <ContentPresenter Grid.Row="1"
                                              ContentSource="SelectedContent"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="18,7" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Foreground" Value="#475467" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Chrome"
                                Height="32"
                                MinWidth="72"
                                CornerRadius="14"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="1">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#F9FAFB" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#FFFFFF" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#D0D5DD" />
                                <Setter Property="Foreground" Value="#175CD3" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Background" Value="#F7FFFFFF" />
            <Setter Property="BorderBrush" Value="#EAECF0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,0,14" />
            <Setter Property="Effect" Value="{StaticResource PanelShadow}" />
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="78" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Background" Value="#1570EF" />
            <Setter Property="BorderBrush" Value="#1570EF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                CornerRadius="14"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#175CD3" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#175CD3" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#1849A9" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#1849A9" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Background" Value="#B2DDFF" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#B2DDFF" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="12"
            CornerRadius="24"
            Background="#F5F7FA"
            BorderBrush="#E4E7EC"
            BorderThickness="1"
            Effect="{StaticResource WindowShadow}"
            MouseLeftButtonDown="WindowSurface_MouseLeftButtonDown">
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Margin="2,0,2,24"
                       MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Button DockPanel.Dock="Right"
                        Style="{StaticResource WindowCloseButton}"
                        Content="×"
                        Click="Close_Click" />
                <StackPanel>
                    <TextBlock Text="设置"
                               FontSize="22"
                               FontWeight="SemiBold"
                               Foreground="#101828" />
                    <TextBlock Text="外观、窗口行为、自动翻页和快捷键"
                               Margin="0,4,0,0"
                               Foreground="#667085" />
                </StackPanel>
            </DockPanel>

            <TabControl Grid.Row="1" Padding="0" Margin="0,10,0,0">
                <TabItem Header="外观">
                    <ScrollViewer VerticalScrollBarVisibility="Disabled" Padding="0,8,0,0">
                        <StackPanel>
                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                        <TextBlock Text="文字颜色" Width="72" />
                                        <Border x:Name="TextColorPreview" Style="{StaticResource ColorPreview}" />
                                        <Button Content="选择" Click="PickTextColor_Click" />

                                        <TextBlock Text="背景颜色" Width="72" Margin="28,0,0,0" />
                                        <Border x:Name="BackgroundColorPreview" Style="{StaticResource ColorPreview}" />
                                        <Button Content="选择" Click="PickBackgroundColor_Click" />
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="字号" Width="72" />
                                            <Button Style="{StaticResource StepButton}" Content="−" Click="DecreaseFontSize_Click" />
                                            <TextBox x:Name="FontSizeValueBox"
                                                     Style="{StaticResource FontSizeTextBox}"
                                                     KeyDown="FontSizeValueBox_KeyDown"
                                                     LostFocus="FontSizeValueBox_LostFocus" />
                                            <Button Style="{StaticResource StepButton}" Content="+" Click="IncreaseFontSize_Click" />
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="34,0,0,0">
                                            <TextBlock Text="行高" Width="52" />
                                            <Button Style="{StaticResource StepButton}" Content="−" Click="DecreaseLineHeight_Click" />
                                            <TextBox x:Name="LineHeightValueBox"
                                                     Style="{StaticResource FontSizeTextBox}"
                                                     KeyDown="LineHeightValueBox_KeyDown"
                                                     LostFocus="LineHeightValueBox_LostFocus" />
                                            <Button Style="{StaticResource StepButton}" Content="+" Click="IncreaseLineHeight_Click" />
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Card}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="126" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="背景不透明度" />
                                    <Slider x:Name="BackgroundOpacitySlider" Grid.Column="1" Minimum="0.01" Maximum="1" Margin="0,0,0,12" ValueChanged="OpacitySlider_ValueChanged" />
                                    <TextBox x:Name="BackgroundOpacityValueBox" Grid.Column="2" Style="{StaticResource ValueTextBox}" KeyDown="OpacityValueBox_KeyDown" LostFocus="OpacityValueBox_LostFocus" />

                                    <TextBlock Grid.Row="1" Text="整窗不透明度" />
                                    <Slider x:Name="WindowOpacitySlider" Grid.Row="1" Grid.Column="1" Minimum="0.01" Maximum="1" Margin="0,0,0,12" ValueChanged="OpacitySlider_ValueChanged" />
                                    <TextBox x:Name="WindowOpacityValueBox" Grid.Row="1" Grid.Column="2" Style="{StaticResource ValueTextBox}" KeyDown="OpacityValueBox_KeyDown" LostFocus="OpacityValueBox_LostFocus" />

                                    <TextBlock Grid.Row="2" Text="悬隐不透明度" />
                                    <Slider x:Name="HiddenOpacitySlider" Grid.Row="2" Grid.Column="1" Minimum="0.01" Maximum="1" ValueChanged="OpacitySlider_ValueChanged" />
                                    <TextBox x:Name="HiddenOpacityValueBox" Grid.Row="2" Grid.Column="2" Style="{StaticResource ValueTextBox}" Margin="10,0,0,0" KeyDown="OpacityValueBox_KeyDown" LostFocus="OpacityValueBox_LostFocus" />
                                </Grid>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="窗口">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,14,0,0">
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <Border Margin="0,0,0,12"
                                        Padding="12,10"
                                        CornerRadius="12"
                                        Background="#F7FCFF"
                                        BorderBrush="#D7EEFF"
                                        BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="强行显示窗口" FontWeight="SemiBold" Foreground="#1D2939" />
                                        <TextBlock Grid.Column="1"
                                                   Text="Ctrl+Shift+F12"
                                                   Foreground="#1570EF"
                                                   FontWeight="SemiBold"
                                                   Margin="12,0,8,0" />
                                        <Button x:Name="ForceShowHelpButton"
                                                Grid.Column="2"
                                                Content="?"
                                                Width="22"
                                                Height="22"
                                                MinWidth="22"
                                                Padding="0"
                                                FontWeight="Bold"
                                                Foreground="#1570EF"
                                                Background="#EAF7FF"
                                                BorderBrush="#B7E3FF"
                                                Click="ForceShowHelp_Click" />
                                    </Grid>
                                </Border>
                                <Popup x:Name="ForceShowHelpPopup"
                                       PlacementTarget="{Binding ElementName=ForceShowHelpButton}"
                                       Placement="Right"
                                       StaysOpen="False"
                                       AllowsTransparency="True">
                                    <Border Width="310"
                                            Margin="8"
                                            Padding="12"
                                            CornerRadius="14"
                                            Background="#F8FFFFFF"
                                            BorderBrush="#D7EEFF"
                                            BorderThickness="1"
                                            Effect="{StaticResource PanelShadow}">
                                        <TextBlock TextWrapping="Wrap"
                                                   LineHeight="22"
                                                   Foreground="#344054"
                                                   Text="按下Ctrl+Shift+F12后将强制显示窗口，背景及窗口不透明度恢复为100%，功能栏隐藏取消。" />
                                    </Border>
                                </Popup>
                                <CheckBox x:Name="TopmostBox" Content="窗口置顶" />
                                <CheckBox x:Name="HoverHideBox" Content="鼠标移出后悬隐" />
                                <CheckBox x:Name="ToolbarVisibleBox" Content="显示功能栏" />
                                <CheckBox x:Name="MinimizeToTrayBox" Content="最小化到托盘" />
                            </StackPanel>
                        </Border>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="翻页">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,14,0,0">
                        <Border Style="{StaticResource Card}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <CheckBox x:Name="AutoPageEnabledBox"
                                          Grid.ColumnSpan="2"
                                          Content="启动时开启自动翻页"
                                          Margin="0,0,0,16" />

                                <CheckBox x:Name="PageTurnByPageBox"
                                          Grid.Row="1"
                                          Grid.ColumnSpan="2"
                                          Content="使用整页翻页"
                                          Margin="0,0,0,16" />

                                <TextBlock Grid.Row="2" Text="翻页间隔（秒）" />
                                <TextBox x:Name="AutoPageIntervalBox"
                                         Grid.Row="2"
                                         Grid.Column="1"
                                         Width="140"
                                         HorizontalAlignment="Left" />
                            </Grid>
                        </Border>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="快捷键">
                    <DockPanel Margin="0,14,0,0">
                        <Border Style="{StaticResource Card}" Margin="0">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl x:Name="ShortcutList" />
                            </ScrollViewer>
                        </Border>
                    </DockPanel>
                </TabItem>
            </TabControl>

            <Border Grid.Row="2"
                    Margin="0,14,0,0"
                    Padding="12"
                    CornerRadius="18"
                    Background="#EFFFFFFF"
                    BorderBrush="#EAECF0"
                    BorderThickness="1">
                <DockPanel LastChildFill="False">
                    <Button Content="恢复默认快捷键"
                            Width="130"
                            DockPanel.Dock="Left"
                            Click="ResetShortcuts_Click" />
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Button Content="应用" Width="82" Margin="0,0,8,0" Click="Apply_Click" />
                        <Button Content="保存" Width="82" Margin="0,0,8,0" Style="{StaticResource PrimaryButton}" Click="Save_Click" />
                        <Button Content="关闭" Width="82" Click="Close_Click" />
                    </StackPanel>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</Window>
